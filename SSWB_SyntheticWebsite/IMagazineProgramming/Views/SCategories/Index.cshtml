@model IEnumerable<IMagazineProgramming.Models.SCategory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Share/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameSCategory)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descibe)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.XCategory.NameXCategory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NameSCategory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descibe)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.XCategory.NameXCategory)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
